engine: 0
temperature: 0.7
max_tokens: 100
version: 0
reminder: 'This uses the weakest engine by default.'
doc: |-
  Short prompt that only uses the user's input with no additional text. You can pass in an empty string to get a truly empty prompt (I believe gpt will start with the "<|endoftext|>" token in that case).
prompt: |-
    {}
